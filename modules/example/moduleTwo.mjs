// import printMyName from './moduleOne.js' - импорт одного элемента

import {
    myName,
    sum
} from './moduleOne.mjs'
// имопрт нескольких элементов

myName()

const res = sum(10, 5)
console.log(res)